# wordcloud2_stata
Interactive word cloud generator with collision-free layout using Stata. It reads a string variable containing free text, counts word frequencies across all observations, and produces an interactive HTML/SVG word cloud. Words are sized proportionally to their frequency and placed using a collision-free Archimedean spiral layout algorithm.
